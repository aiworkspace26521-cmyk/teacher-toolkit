# 《招式培養系統 v3.0 升級改造白皮書》

> 寶可夢招式系統 · D4 點數制 · 形態過濾 · 個人化技能樹
> 版本：v3.0 ｜ 狀態：設計定稿 ｜ 日期：2026-08-20

---

## 摘要

現行招式系統（v2.0）採用「演算法自動判定變體（Variant-First）」機制，由種族值與性格自動決定招式路徑。經實測確認三大致命問題：

1. **玩家無自主權**：招式路徑不可控，喪失養成樂趣與決策深度。
2. **同質化嚴重**：全圖鑑 800+ 隻寶可夢中，同屬性共用同一技能池與技能樹，收集與培養價值低。
3. **生理合理性衝突**：蹄類馬型（小火馬）可學「火焰拳／雷電拳」，爪型龍型（火恐龍）可學「二連踢／飛踢」，嚴重破壞世界觀。

v3.0 以「**玩家主導配點制（D4 精神）**」為核心，導入三大改造支柱：

| 支柱 | 解決問題 | 核心機制 |
|------|----------|----------|
| **D4 配點制** | 玩家無自主權 | 每隻寶可夢累計 60 SP，手動解鎖 T1~T5 階層、升級 Lv.1~Lv.5、選擇質變分支 |
| **形態標籤過濾器** | 同質化／生理衝突 | `Morphology Tags` 依寶可夢生理特徵動態生成專屬技能樹 |
| **Signature Affinity（專屬加成）** | 收集樂趣低落 | 傳說／御三家／特色寶可夢具專屬 Keystone 節點，每隻的最佳流派唯一 |

改造採「**增量遷移**」：保留既有 `TYPE_SPEC_V2`、`SPECIES_LEARNSET`、`ULT_VARIANTS` 資料欄位，僅擴充而非重寫；演算法變體權重平滑轉移為玩家可見的技能樹節點。以 **火屬性為完整示範**，驗證 6 大戰術流派與 4 種形態寶可夢的差異化呈現。

---

## 名詞對照表

| 術語 | 說明 |
|------|------|
| **SP（Skill Points）** | 技能點數，每隻寶可夢升級累計共 60 點，玩家手動配置 |
| **Track（軌）** | 技能樹五條路徑：ATK／SPA／BUF／DIS／ULT |
| **Tier（階層）** | 每軌內 T1~T5 五階，愈高階節點愈強但所需 SP 愈高 |
| **Modifier（質變分支）** | 招式升至 Lv.5 時解鎖的 2 選 1 進階改造 |
| **Morphology Tag（形態標籤）** | 描述寶可夢生理特徵的標籤（如 `BIPEDAL_CLAW`） |
| **Required_Tags** | 招式對寶可夢形態的最低要求；不滿足則不渲染該節點 |
| **Keystone（基石節點）** | 各軌 T5 的終點節點；傳說／御三家專屬 | 
| **Signature Affinity（專屬親和）** | 特定寶可夢對特定招式／屬性的加成係數 |

---

# 第一章：系統架構設計與「生理形態過濾」機制

## 1.1 設計原則

v3.0 的架構設計遵循五項原則：

1. **玩家決策權至上**：所有配點、升級、分支選擇皆由玩家手動執行，系統只負責「提供選項」與「驗證合法性」。
2. **單一來源真理**：招式數值、成長曲線、質變分支一律集中於資料層，渲染與戰鬥結算共享同一份定義，杜絕重複定義（解決 V2 中 3b/3c/3d/3g 重疊問題）。
3. **形態先驗過濾**：技能樹的「可視範圍」在渲染時依寶可夢 Tags 過濾，玩家永遠看不到不合生理的選項（小火馬的技能樹裡根本不存在「火焰拳」）。
4. **增量相容**：既有資料結構原封不動地保留與擴充，舊檔舊進度可無痛轉移。
5. **事件溯源**：所有配點行為寫入不可變事件流（沿用既有 `kpi_events` 模式），支援重播、稽核與安全回滾。

## 1.2 三層資料架構

```
┌─────────────────────────────────────────────┐
│  L1  全域屬性總池（18 Attribute Libraries）   │  定義該屬性完整 5 軌 × T1~T5 招式庫
│        fire: { ATK:{T1..T5}, SPA:{..}, ... } │
├─────────────────────────────────────────────┤
│  L2  形態標籤層（Morphology Tag System）       │  招式標註 required_tags
│        MOVE_SPEC['火焰拳'].required_tags      │  寶可夢標註 species.tags
│        = ['BIPEDAL_CLAW']                    │
├─────────────────────────────────────────────┤
│  L3  個體差異層（Signature Affinity）          │  每隻寶可夢的專屬 Keystone 與加成
│        charmander.keystone = '龍焰協奏'        │
└─────────────────────────────────────────────┘
        ↓ 動態解析（resolveSkillTree）
  個人化技能樹（每一隻都不一樣）
```

- **L1（全域總池）**：18 屬性各定義「5 軌 × T1~T5」完整招式庫（v2 的 `TIER_VARIANTS` 擴充為 `TIER_MATRIX`）。此層不綁定任何寶可夢，是「最大公因數」。
- **L2（形態標籤）**：在招式上標註 `required_tags`（允許的最低形態條件）。寶可夢身上標註自己的 `tags`。當 `tags ∩ required_tags ≠ ∅` 時該節點可見；否則該節點由同軌同階的「備援招」動態替換。
- **L3（個體差異）**：每隻寶可夢（尤其是傳說與御三家）配置專屬 Keystone 節點與 Signature Affinity 加成，保證「即使通過 L2 過濾後仍會撞招，最終流派也會因加成而不同」。

## 1.3 形態標籤辭典（Morphology Tag Dictionary）

| Tag | 中文 | 允許的動作特徵 | 常駐寶可夢範例 |
|-----|------|----------------|----------------|
| `BIPEDAL_CLAW` | 雙足利爪 | 爪擊、拳擊、撕扯 | 火恐龍、烈焰猴、甲賀忍蛙 |
| `QUADRUPED_HOOF` | 四足蹄類 | 踢擊、衝撞、踩踏 | 小火馬、烈焰馬、大尾立 |
| `QUADRUPED_CLAW` | 四足利爪 | 爪擊、撲擊、撕裂 | 卡蒂狗、風速狗、索爾迦雷歐 |
| `WINGED` | 有翼飛行 | 翼擊、俯衝、風壓 | 鳳王、噴火龍、快龍 |
| `SERPENTINE` | 蛇形身軀 | 纏繞、咬擊、噴吐 | 火焰鳥？(否) 藤藤蛇、大鋼蛇 |
| `BEAST` | 獸牙咬擊 | 咬擊、撕咬、獠牙 | 卡蒂狗、烈焰猴？(否) 火暴獸 |
| `ARMORED` | 甲殼／重裝 | 鎧甲衝撞、火焰輪 | 席多藍恩、噴火駝 |
| `HORN` | 有角 | 角突、角撞 | 卡蒂狗？(否) 大角鹿類 |
| `TAIL` | 尾類 | 尾擊、甩尾 | 火恐龍、小火馬 |
| `LEGEND` | 傳說 | 可解鎖專屬 Keystone | 鳳王、席多藍恩 |

**標籤組合規則**：
- 寶可夢可持有多個 Tag（如席多藍恩 = `QUADRUPED_CLAW` + `ARMORED`）。
- 招式的 `required_tags` 採「任一命中即可（OR）」，代表「具備該動作特徵即可學」。
- 若需「禁學」語意（如「蹄類禁爪拳」），以 `excluded_tags` 表示：`required_tags: []` 且 `excluded_tags: ['BIPEDAL_CLAW']` 表示「任何非爪類都能學」。

## 1.4 動態個人技能樹生成邏輯（Resolve Pipeline）

```
輸入：寶可夢 species_id
  │
  ▼
Step 1 載入全域總池  ── L1：TYPE_SPEC_V3[type][track][tier]
  │
  ▼
Step 2 形態過濾    ── L2：對每個候選招，檢查 species.tags 是否命中 required_tags
  │                   未命中 → 標記為「遮蔽（hidden）」
  ▼
Step 3 遮蔽替補    ── 每個 (track, tier)：若首選被遮蔽，依「備援優先序」挑下一招；
  │                   全部被遮蔽 → 用該軌通用備援招（FALLBACK_BY_TRACK）
  ▼
Step 4 個體客製    ── L3：注入 Keystone 節點 + Signature Affinity 加成
  │
  ▼
Step 5 渲染       ── 輸出完整樹（含已遮蔽節點的「鎖定顯示」提示，向玩家說明該寶可夢不適合此路）
```

**遮蔽處理原則**：被遮蔽的節點**仍然顯示**，但以「🔒 生理不符」鎖定狀態呈現，讓玩家理解「為何沒有」——這同時是世界觀說明的教育機會，避免玩家困惑「為什麼小火馬沒有火焰拳」。

**備援優先序（FALLBACK_BY_TRACK）**：
```
ATK: ['撞擊','電光一閃','摔打']
SPA: ['火花','噴射火焰','覺醒力量']
BUF: ['瞪眼','叫聲','變硬']
DIS: ['煙幕','叫聲','瞪眼']
ULT: ['聚能','火種']  // 蓄能類被動永不遮蔽
```

## 1.5 SP 配點經濟（Point Economy）

| 項目 | SP 成本 | 備註 |
|------|---------|------|
| 解鎖 T1 節點 | 1 | 該軌起點 |
| 解鎖 T2 | 2 | |
| 解鎖 T3 | 3 | |
| 解鎖 T4 | 4 | |
| 解鎖 T5 | 5 | 含 Keystone 節點 |
| 招式升級（Lv.1→2 … Lv.4→5） | 每級 1（共 4） | |
| 解鎖 Lv.5 質變分支 | 3 | 2 選 1 |
| Keystone 啟動（僅傳說／御三家） | 5 | 該軌 T5 上方 |
| 重置（Reset） | 免費 | 100% 返還，寫入 `SKILL_RESET` 事件 |

**總量**：每隻寶可夢從 Lv.1 起，每升 1 級得 1 SP，至 Lv.60 累計 **60 SP**。

**配點張力驗證**：
- 單軌全滿：解鎖 1+2+3+4+5=15 + 升級 5 招×4=20 + 質變 5×3=15 = **50 SP** → 只剩 10 SP 可滴點他軌。
- 這意味「一條主軌點滿」就幾乎用盡資源，玩家必須做出取捨 → 產生重玩與多流派價值，符合 D4「build 多樣性」精神。

## 1.6 雙屬性寶可夢的點數分配與跨系 Build

雙屬性寶可夢（如噴火龍 火／飛行、席多藍恩 火／鋼）可將 60 SP 分配於**兩個屬性樹**：

- **分配規則**：兩系各自獨立計費，但共用同一 60 SP 錢包；任一軌的 T5 需要先點滿該軌 T1~T4（前導條件）。
- **跨系 Build**：玩家可混搭——火系 SPA 軌（輸出）＋ 飛行系 DIS 軌（控場）＋ 鋼系 BUF 軌（防禦），創造獨特 hybrid 組合。
- **跨系融合節點（Fusion Node）**：雙屬性寶可夢的雙 T5 都啟動後，可解鎖「跨系融合節點」取得專屬組合技：
  - 噴火龍：`火之翼（Fire Wing）`＝火系 ATK + 飛行系 ULT 融合，威力、屬性雙判定。
  - 席多藍恩：`熔核鋼甲（Magma Steel）`＝火系 BUF + 鋼系 DIS 融合，反震加灼燒。
- **形態過濾跨系生效**：`tags` 是「寶可夢全域」的，過濾時同時套用於兩系招式池（噴火龍是 `WINGED`，飛行系翼擊與火系翼類招都可用）。

---

# 第二章：火屬性 6 大流派與技能規格完整範例

## 2.1 火屬性全域總池（5 軌 × T1~T5 招式矩陣）

> 附註：v2 的 `TYPE_SPEC.fire` 四池（ATK/SPA/BUF/DIS_POOL）與 `ULT_MAP`（噴射火焰→焚焰放射、大字爆→滅世爆焰）**完整保留**，本矩陣為其增量擴充。

### ATK 軌（物理攻擊）

| Tier | 招式 | `required_tags` | 備註 |
|------|------|------------------|------|
| T1 | 撞擊 | 全部 | 通用 |
| T1 | 抓 | `BIPEDAL_CLAW`／`QUADRUPED_CLAW` | |
| T1 | 二連踢 | `QUADRUPED_HOOF` | |
| T2 | 火焰拳 | `BIPEDAL_CLAW` | 🔒 小火馬遮蔽 |
| T2 | 蓄能焰襲 | `QUADRUPED_HOOF`／`WINGED` | 🔒 火恐龍遮蔽 |
| T2 | 火焰踢 | `BIPEDAL_CLAW` | |
| T2 | 火焰輪 | `QUADRUPED_CLAW`／`ARMORED` | |
| T3 | 炎牙 | `BEAST`／`SERPENTINE` | |
| T3 | 火焰爪 | `BIPEDAL_CLAW`／`QUADRUPED_CLAW` | |
| T3 | 閃焰衝鋒 | 全部（捨身系） | 反作用力 |
| T4 | 大鬧一番 | `BIPEDAL_CLAW` | |
| T4 | 烈焰衝撞 | `QUADRUPED_CLAW`／`ARMORED` | |
| T4 | 龍之俯衝 | `WINGED`／`SERPENTINE` | |
| T5 | V熱焰 | `BIPEDAL_CLAW` | 火系物理終點 |
| T5 | 熔岩衝擊 | `ARMORED`／`QUADRUPED_CLAW` | 岩火物理 |
| T5 | 聖火俯衝 | `WINGED`＋`LEGEND` | 鳳王專屬 |

### SPA 軌（特殊攻擊）

| Tier | 招式 | `required_tags` | 備註 |
|------|------|------------------|------|
| T1 | 火花 | 全部 | 通用 |
| T1 | 火焰旋渦 | 全部 | |
| T2 | 噴射火焰 | 全部 | 通用核心 |
| T2 | 魔法火焰 | `BIPEDAL_CLAW`／`WINGED` | |
| T3 | 大字爆 | 全部 | |
| T3 | 熱風 | `WINGED`／`SERPENTINE` | 🔒 純蹄類遮蔽 |
| T4 | 過熱 | 全部 | 反作用力（特攻 -2） |
| T4 | 熔岩風暴 | `ARMORED`／`SERPENTINE` | |
| T5 | 焚焰放射（ULT） | 全部 | 需先點 ULT 軌 T3 |
| T5 | 滅世爆焰（ULT） | 全部 | 需先點 ULT 軌 T5 |

### BUF 軌（強化／輔助）

| Tier | 招式 | `required_tags` | 備註 |
|------|------|------------------|------|
| T1 | 瞪眼／叫聲 | 全部 | |
| T1 | 搖尾巴 | `QUADRUPED_*`／`TAIL` | |
| T2 | 影子分身 | 全部 | |
| T2 | 高速移動 | `QUADRUPED_HOOF`／`WINGED` | |
| T2 | 煙幕 | 全部 | |
| T3 | 劍舞 | `BIPEDAL_CLAW`／`QUADRUPED_CLAW` | |
| T3 | 龍之舞 | `WINGED`／`SERPENTINE`／`BEAST` | |
| T3 | 健美 | `BIPEDAL_CLAW`／`QUADRUPED_*` | |
| T4 | 大晴天 | 全部 | 天氣核心 |
| T4 | 肚鼓 | 全部 | 自殘系 |
| T4 | 陽焰 | `WINGED`／`SERPENTINE` | 太陽之力前置 |
| T5 | 太陽領域（Keystone） | `WINGED`＋`LEGEND` | 日照領域流終點 |
| T5 | 燃燒解放（ULT BUF） | 全部 | 需 ULT 軌 T4 |

### DIS 軌（干擾／控場）

| Tier | 招式 | `required_tags` | 備註 |
|------|------|------------------|------|
| T1 | 煙幕 | 全部 | |
| T1 | 瞪眼 | 全部 | |
| T2 | 火焰旋渦 | 全部 | 束縛 DoT |
| T2 | 鬼火 | 全部 | 燃燒 2 層 |
| T3 | 清除之煙 | `SERPENTINE`／`WINGED` | |
| T3 | 怪異之光 | `WINGED`／`BEAST` | |
| T4 | 吹飛 | 全部 | |
| T4 | 烈焰詛咒 | `BEAST`／`SERPENTINE` | 燃燒＋降攻 |
| T5 | 滅亡之歌 | `LEGEND` | 傳限 |
| T5 | 焚燒領域（Keystone） | `LEGEND` | 全場持續灼燒 |

### ULT 軌（奧義蓄能／終極技）

| Tier | 節點 | 類型 | 說明 |
|------|------|------|------|
| T1 | 聚能 | 被動 | 能量累積效率 +5% |
| T2 | 火種 | 被動 | 灼燒層數上限 +1 |
| T3 | 焚焰放射 | 主動 | 火系中距終極技（v2 ULT_MAP 原樣納入） |
| T4 | 焰火共鳴 | 被動 | ULT 傷害 +20% |
| T5 | 滅世爆焰／聖火重生 | 主動 | 大範圍終極技；傳說寶可夢置換為專屬奧義 |

## 2.2 6 大戰術流派（火屬性）

每流派給出：定位、核心機制、關鍵節點路徑、最佳形態、關鍵質變。

### 流派 1：焦土灼燒流（DoT 疊加與引爆）

- **定位**：持續傷害消耗、拖回合。
- **核心機制**：疊加「燃燒層數」並以 `焚焰放射`／`滅世爆焰` 引爆，一次性結算所有層數傷害。
- **關鍵節點**：DIS T2 `鬼火` → DIS T4 `烈焰詛咒` → ULT T3 `焚焰放射`（引爆核心）→ ULT T2 `火種`（層數上限 +1）。
- **最佳形態**：卡蒂狗／風速狗（`BEAST` 可學烈焰詛咒）、鳳王（`LEGEND` 可學焚燒領域）。
- **推薦質變**：噴射火焰 Lv.5 → `焦油重燃`（DoT +1 回合、灼燒傷害 +30%）。

### 流派 2：聚能爆殺流（單體特攻穿透核彈）

- **定位**：爆發單點、穿透坦。
- **核心機制**：SPA 軌點滿，疊 `焰火共鳴` 增傷，`過熱` 後接 `大字爆`。
- **關鍵節點**：SPA T2 `噴射火焰` → T3 `大字爆` → T4 `過熱` → ULT T4 `焰火共鳴`。
- **最佳形態**：任何形態皆可；席多藍恩（`ARMORED` 有 `熔岩風暴` T4）與噴火龍最佳。
- **推薦質變**：噴射火焰 Lv.5 → `焚焰穿透`（無視 25% 特防，對灼燒目標 40%）。

### 流派 3：自殘狂戰流（反作用力加成與殘血爆發）

- **定位**：高風險高報酬、殘血斬殺。
- **核心機制**：使用帶反作用力的招式（`閃焰衝鋒`、`過熱`、`V熱焰`），配合「殘血增傷」類 Buff 與 `肚鼓`。
- **關鍵節點**：BUF T4 `肚鼓` → ATK T3 `閃焰衝鋒` → ATK T5 `V熱焰`。
- **最佳形態**：火恐龍（`BIPEDAL_CLAW` 學 `V熱焰`）、烈焰猴。
- **推薦質變**：V熱焰 Lv.5 → `燃盡之魂`（HP<30% 時威力 ×1.5，反作用力減半）。

### 流派 4：日照領域流（晴天天氣與全隊增益）

- **定位**：天氣主導、團隊增益。
- **核心機制**：`大晴天` 開啟天氣 → `陽焰`／`太陽領域` 持續增益 → 火系傷害強化 + 水系減半。
- **關鍵節點**：BUF T4 `大晴天` → BUF T4 `陽焰` → BUF T5 `太陽領域`（Keystone）。
- **最佳形態**：鳳王（`WINGED`＋`LEGEND`）、火暴獸（`QUADRUPED_CLAW`）。
- **推薦質變**：大晴天 Lv.5 → `永晝`（天氣持續回合 +2）。

### 流派 5：炎壁反擊流（熔岩護盾與防禦反震坦）

- **定位**：防禦反震、坦傷。
- **核心機制**：BUF 防禦疊層 → 護盾吸收 → 受擊時反震灼燒。
- **關鍵節點**：BUF T3 `健美` → DIS T4 `烈焰詛咒`（降攻）→ ATK T5 `熔岩衝擊`。
- **最佳形態**：席多藍恩（`ARMORED`，本流派唯一完整體）、噴火駝。
- **推薦質變**：熔岩衝擊 Lv.5 → `熔核反震`（受擊時反擊 30% 傷害＋灼燒）。

### 流派 6：極速突襲流（先制打擊、音速收割與遊擊打法）

- **定位**：先制收割、游擊拉扯。
- **核心機制**：速度加成疊層 → 先制招式 → 擊殺後再動（音速收割）。
- **關鍵節點**：BUF T2 `高速移動` → ATK T2 `蓄能焰襲`（疊速）→ 電光一閃（通用先制）。
- **最佳形態**：小火馬／烈焰馬（`QUADRUPED_HOOF` 的唯一完整體）。
- **推薦質變**：蓄能焰襲 Lv.5 → `焰蹄疾馳`（速度 +2 階，擊殺再 +1）。

> **形態 × 流派映射驗證**：6 個流派各自的「最佳形態」幾乎不重疊（DoT→犬系、爆殺→龍爪系、狂戰→爪系、日照→翼系、反擊→甲系、突襲→蹄系）。這正是「收集 6 隻火系各玩一種」的核心保證。

## 2.3 不同形態寶可夢的技能樹呈現差異

以 **ATK 軌 T2、SPA 軌 T3、BUF 軌 T5** 三列為對照，展示同一屬性樹在 4 隻寶可夢身上的不同呈現：

| 寶可夢 | 形態標籤 | ATK T2 | SPA T3 | BUF T5 | Keystone |
|--------|----------|--------|--------|--------|----------|
| **火恐龍** | `BIPEDAL_CLAW`＋`TAIL` | 🔥火焰拳 | 大字爆 | 燃燒解放 | `龍焰協奏`（火＋龍，`V熱焰` 威力 +30%） |
| **小火馬** | `QUADRUPED_HOOF` | 🔒火焰拳（遮蔽）→ **蓄能焰襲** | 🔒熱風（遮蔽）→ 大字爆 | 燃燒解放 | `焰蹄疾馳`（速度疊層上限 +2） |
| **席多藍恩** | `QUADRUPED_CLAW`＋`ARMORED` | **火焰輪** | **熔岩風暴** | 太陽領域（可） | `熔核鋼甲`（反震＋灼燒） |
| **鳳王** | `WINGED`＋`LEGEND` | 🔒火焰拳（遮蔽）→ 聖火俯衝 | **熱風** | **太陽領域** | `聖火重生`（倒地 1 次／戰，滿血復活） |

**重點**：
- 火恐龍的 ATK T2 直接顯示 `火焰拳`；小火馬該格為 🔒 遮蔽並替補 `蓄能焰襲`；兩隻「同時按 ATK 鍵」看到的樹完全不同。
- 席多藍恩因 `ARMORED` 獲得獨佔的 `熔岩風暴`／`熔核鋼甲`；鳳王因 `WINGED＋LEGEND` 獨佔 `聖火重生` 與 `太陽領域`。
- **同一條火系樹 → 四隻四種長相**，玩家為湊齊 4 種玩法必須收集 4 隻。

## 2.4 3 個招式的 Lv.1~Lv.5 成長數值與 D4 質變分支

### 範例 A：火焰拳（Fire Punch）— ATK 軌／`BIPEDAL_CLAW`

| 等級 | 威力 | 命中 | 灼燒率 | 累計 SP | 附加效果 |
|------|------|------|--------|---------|----------|
| Lv.1 | 70 | 100 | 8% | 1（解鎖） | 無 |
| Lv.2 | 78 | 100 | 10% | 2 | 無 |
| Lv.3 | 88 | 100 | 12% | 3 | 對鋼系命中率 +10 |
| Lv.4 | 98 | 100 | 15% | 4 | 擊中要害率 +1 階 |
| Lv.5 | 105 | 100 | 18% | 5 | **解鎖質變分支（2 選 1，各 +3SP）** |

**Lv.5 質變分支**：
| 分支 | 效果 |
|------|------|
| 🔥 **熔核之拳** | 傷害 +10%；命中後施加 **2 層**灼燒（單發雙層） |
| ⚡ **雙連炎拳** | 變二連擊（每擊 55% 威力）；擊中要害率 +2 階 |

### 範例 B：噴射火焰（Flamethrower）— SPA 軌／通用

| 等級 | 威力 | 特防穿透 | 灼燒率 | 累計 SP | 附加效果 |
|------|------|----------|--------|---------|----------|
| Lv.1 | 90 | 0% | 10% | 1（解鎖） | 無 |
| Lv.2 | 95 | 0% | 12% | 2 | 無 |
| Lv.3 | 100 | 5% | 14% | 3 | 無 |
| Lv.4 | 105 | 10% | 16% | 4 | 無 |
| Lv.5 | 110 | 15% | 20% | 5 | **解鎖質變分支** |

**Lv.5 質變分支**：
| 分支 | 效果 |
|------|------|
| 🔥 **焚焰穿透** | 無視目標 25% 特防；對已灼燒目標改為無視 40% |
| 🌋 **焦油重燃** | 灼燒持續回合 +1；灼燒 DoT 傷害 +30%（適合焦土灼燒流） |

### 範例 C：蓄能焰襲（Flame Charge）— ATK／BUF 軌／`QUADRUPED_HOOF`＋`WINGED`

| 等級 | 威力 | 速度強化 | 灼燒率 | 累計 SP | 附加效果 |
|------|------|----------|--------|---------|----------|
| Lv.1 | 45 | +1 階 | 0% | 1（解鎖） | 無 |
| Lv.2 | 50 | +1 階 | 0% | 2 | 無 |
| Lv.3 | 55 | +1 階 | 5% | 3 | 無 |
| Lv.4 | 60 | +1 階 | 8% | 4 | 命中要害率 +1 |
| Lv.5 | 65 | +1 階 | 10% | 5 | **解鎖質變分支** |

**Lv.5 質變分支**：
| 分支 | 效果 |
|------|------|
| 🏇 **焰蹄疾馳** | 速度強化提升至 **+2 階**；擊殺敵人後再額外 +1 階（音速收割） |
| 🔥 **焰風殘影** | 命中後 30% 機率本回合無視對手招式（閃避遊擊） |

---

# 第三章：800+ 寶可夢防重複體驗設計

## 3.1 三階層差異化設計

為避免「800+ 隻 = 800 組參數」的維護災難，採用「分層規則 + 個別覆寫」：

| 階層 | 代表 | 形態標籤 | Signature Affinity | Keystone | 維護方式 |
|------|------|----------|--------------------|----------|----------|
| **一般寶可夢** | 小火馬、卡蒂狗 | 依生理標註（1~3 個） | 無 | 無（共用軌 T5 節點） | 全自動，只需標 tags |
| **御三家** | 火恐龍系、烈焰猴系 | 依生理標註 | 御三家專屬「本系招式威力 +15%」 | 專屬 Keystone（如 `龍焰協奏`） | 自動 + 1 個 keystone 欄位 |
| **傳說寶可夢** | 鳳王、席多藍恩 | 依生理標註（至少 2 個） | 專屬多屬性加成 | 專屬 2 個 Keystone 二選一 | 自動 + 1~2 個 keystone 欄位 |

**防重複的第一道保險是形態**：800+ 隻只要標好 tags，自動過濾器就讓同屬性的每隻「看到的樹」不同。

**防重複的第二道保險是 Affinity**：即使兩隻都是 `BIPEDAL_CLAW`（火恐龍、烈焰猴都能學火焰拳），各自的 Keystone 也讓「火焰拳+誰」的終局組合不同：
- 火恐龍 → `龍焰協奏` 讓 `V熱焰` 變雙屬性（火＋龍），走狂戰。
- 烈焰猴 → `猛火魂` 讓 `火焰拳` 在殘血時威力 ×1.5，走殘血爆發。

## 3.2 Signature Affinity 與 Keystone 規則

1. **Affinity 為「係數」，不新增招式**：它調整現有招式的傷害／效果（威力 +%、灼燒層數、反作用力減免、速度疊層），因此不會造成招式膨脹。
2. **Keystone 為「節點」，位於軌 T5 之上**：需先點滿該軌 T1~T5（前導 15 SP）＋ 5 SP 啟動；每隻寶可夢最多啟動 2 顆。
3. **禁制條款**：Keystone 不可重複（每隻寶可夢唯一）、不可交易、重製時返還 SP。
4. **規則優先序**：`Affinity > Modifier > 基礎數值`。質變分支與專屬加成疊加時，先套 Modifier 再套 Affinity。

## 3.3 5 隻火系寶可夢的完全不重複玩法示範

| 寶可夢 | 形態 | 推薦流派 | 為什麼唯一 |
|--------|------|----------|-----------|
| **小火馬** | `QUADRUPED_HOOF` | 極速突襲流 | 唯一能學 `蓄能焰襲＋焰蹄疾馳` 完整先制體；爪拳全遮蔽 |
| **卡蒂狗** | `QUADRUPED_CLAW`＋`BEAST` | 焦土灼燒流 | 唯一 `炎牙＋烈焰詛咒` 物理 DoT 體；`BEAST` 專屬 |
| **火恐龍** | `BIPEDAL_CLAW`＋`TAIL` | 聚能爆殺／自殘狂戰 | 唯一 `V熱焰＋龍焰協奏` 雙屬性終點 |
| **席多藍恩** | `QUADRUPED_CLAW`＋`ARMORED` | 炎壁反擊流 | 唯一 `熔岩風暴＋熔核鋼甲` 反震坦；鋼系副樹可再點防禦 |
| **鳳王** | `WINGED`＋`LEGEND` | 日照領域流 | 唯一 `熱風＋太陽領域＋聖火重生` 團隊天氣核心 |

> **驗證**：這 5 隻同屬火系，但各自的最佳流派互斥、最佳招式池幾乎無交集（除通用招外）、Keystone 各自唯一。玩家若要體驗 5 種火系玩法，就必須收集 5 隻——收集與培養價值由此建立。

## 3.4 防重複設計檢查清單（Design Checklist）

新增任何寶可夢時依序檢查，全部通過才放行：
- [ ] 已標註生理 tags（至少 1 個；傳說至少 2 個）
- [ ] 形態過濾器試跑：該屬性樹中「遮蔽節點 ≥ 2 個」（代表不是萬能型）
- [ ] 與同屬性其他寶可夢比對：最佳流派是否重疊？（重疊則需加 Affinity）
- [ ] 是否具有至少 1 個獨佔招式或獨佔質變（Optional for 一般、Required for 傳說）
- [ ] 傳說寶可夢：Keystone 效果不得與現有傳說重複

---

# 第四章：逐步程式碼重構執行計劃（Step-by-Step Migration Plan）

> 以下以 **JavaScript**（既有前端／後端語言）為主，並附 Python 對照。所有改動都遵守「增量遷移、事件溯源、功能旗標可回滾」。

## Phase 1：資料結構增量遷移（Data Schema Migration）

**目標**：在既有 `TYPE_SPEC_V2` 上擴充，不破壞舊欄位。

### 1.1 資料結構擴充

```js
// ── 既有 v2 結構（原樣保留） ──
const TYPE_SPEC_V2 = {
  fire: {
    ATK_POOL: ['火花','火焰拳','火焰踢','閃焰衝鋒','V熱焰'],
    SPA_POOL: ['火花','噴射火焰','大字爆','過熱','熱風','魔法火焰'],
    BUF_POOL: ['大晴天','劍舞','龍之舞','健美','守住','替身','腹鼓','睡覺'],
    DIS_POOL: ['煙幕','瞪眼','鬼火','火焰旋渦','怪異之光','清除之煙','吹飛','滅亡之歌'],
    ULT_MAP: { '噴射火焰':'焚焰放射', '大字爆':'滅世爆焰' },
    TIER_VARIANTS: { /* ... */ }
  }
};

// ── v3.0 增量擴充：新增欄位，不刪舊欄位 ──
const TYPE_SPEC_V3 = Object.fromEntries(
  Object.entries(TYPE_SPEC_V2).map(([type, spec]) => [
    type,
    {
      ...spec,                                // 保留 ATK_POOL / SPA_POOL / ULT_MAP / TIER_VARIANTS
      TIER_MATRIX: TREE_TIERS[type],          // 新：5 軌 × T1~T5（取代 TIER_VARIANTS 的生成角色）
      MOVE_SPEC: MOVE_SPECS[type],            // 新：每招 Lv.1~5 成長 + lv5 質變
    },
  ])
);
```

### 1.2 招式規格（含形態限制與質變分支）

```js
// fire MOVE_SPEC 範例（火焰拳）
const MOVE_SPECS = {
  fire: {
    '火焰拳': {
      category: 'ATK',
      type: 'fire',
      required_tags: ['BIPEDAL_CLAW', 'QUADRUPED_CLAW'], // 形態過濾
      excluded_tags: ['QUADRUPED_HOOF', 'SERPENTINE'],
      base: { power: 70, acc: 100 },
      growth: { power: [70, 78, 88, 98, 105], burn: [0.08, 0.10, 0.12, 0.15, 0.18] },
      lv5_modifiers: {
        '熔核之拳': { damage_mult: 1.1, burn_layers: 2 },
        '雙連炎拳': { multi_hit: 2, per_hit_power: 0.55, crit_bonus: 2 },
      },
    },
    // ...其餘招式
  },
};
```

### 1.3 寶可夢形態標籤（擴充 `SPECIES_LEARNSET`）

```js
// 既有 SPECIES_LEARNSET 只含 learnset，擴充一個 tags 欄位
const SPECIES_LEARNSET = {
  // 既有條目（含 learnset 陣列）...
  charmander: { id: 4, type: ['fire'], tags: ['BIPEDAL_CLAW', 'TAIL'], learnset: ['火花','抓','瞪眼'] },
  ponyta:     { id: 77, type: ['fire'], tags: ['QUADRUPED_HOOF'],            learnset: ['火花','撞擊','搖尾巴'] },
  heatran:    { id: 485, type: ['fire','steel'], tags: ['QUADRUPED_CLAW','ARMORED'], learnset: ['火花','咬住'] },
  hooh:       { id: 250, type: ['fire','flying'], tags: ['WINGED','LEGEND'], learnset: ['神聖火焰','熱風'] },
};
```

### 1.4 Python 對照

```python
TYPE_SPEC_V2 = { "fire": { "ATK_POOL": [...], "ULT_MAP": {...} } }
TYPE_SPEC_V3 = {
    t: { **spec, "TIER_MATRIX": TREE_TIERS[t], "MOVE_SPEC": MOVE_SPECS[t] }
    for t, spec in TYPE_SPEC_V2.items()
}
```

### 1.5 驗收

- [ ] 既有 `TYPE_SPEC_V2` 欄位全數保留，任何舊讀取路徑不報錯。
- [ ] 全 18 屬性皆有 `TIER_MATRIX` 與 `MOVE_SPEC`（火屬性為完整範例，其餘可先以「v2 池映射 + 無質變」墊底）。
- [ ] 100% 圖鑑（至少核心樣本）完成 tags 標註。

## Phase 2：技能樹動態生成器（Skill Tree Resolver Engine）

**目標**：依 `species_id + tags` 動態生成個人化 5 軌天賦樹。

### 2.1 備援招定義

```js
const FALLBACK_BY_TRACK = {
  ATK: ['撞擊', '電光一閃', '摔打'],
  SPA: ['火花', '覺醒力量'],
  BUF: ['瞪眼', '叫聲', '變硬'],
  DIS: ['煙幕', '叫聲'],
  ULT: ['聚能', '火種'],
};
```

### 2.2 Resolver 核心

```js
function resolveSkillTree(speciesId) {
  const species = SPECIES_LEARNSET[speciesId];
  if (!species) throw new Error(`unknown species: ${speciesId}`);

  const tree = { tracks: {}, keystones: [] };

  for (const type of species.type) {                       // 雙屬性兩棵樹都會生成
    const lib = TYPE_SPEC_V3[type];
    for (const track of ['ATK', 'SPA', 'BUF', 'DIS', 'ULT']) {
      for (const tier of ['T1','T2','T3','T4','T5']) {
        const candidates = lib.TIER_MATRIX[track][tier];
        const pick = pickEligibleMove(candidates, species.tags, lib);
        tree.tracks[`${type}:${track}:${tier}`] = pick;
      }
    }
    // Keystone 注入（個體差異層）
    if (species.keystone) tree.keystones.push({ ...species.keystone, type });
  }
  return tree;
}

// 形態過濾 + 遮蔽替補
function pickEligibleMove(candidates, tags, lib) {
  for (const moveName of candidates) {
    const spec = lib.MOVE_SPEC[moveName];
    if (!spec) continue;
    if (isEligible(tags, spec)) return { name: moveName, locked: false };
  }
  const fb = FALLBACK_BY_TRACK[/* track */].find(m => lib.MOVE_SPEC[m]);
  return { name: fb, locked: false, fallback: true };
}

function isEligible(tags, spec) {
  const hasReq = !spec.required_tags?.length || spec.required_tags.some(t => tags.includes(t));
  const notExcluded = spec.excluded_tags ? !spec.excluded_tags.some(t => tags.includes(t)) : true;
  return hasReq && notExcluded;
}
```

### 2.3 渲染遮蔽提示

```js
// 被遮蔽的節點以 locked=true 顯示「🔒 生理不符」
function renderTree(speciesId) {
  const tree = resolveSkillTree(speciesId);
  return tree.tracks.map((node, slot) => ({
    ...node,
    ui: {
      locked: node.locked,
      lockReason: node.locked ? 'MORPHOLOGY_MISMATCH' : null,
      lockedMove: node.locked ? node.originalName : null, // 顯示「本應是火焰拳」
    },
  }));
}
```

### 2.4 Python 對照（略同，純函式，無狀態）

### 2.5 驗收

- [ ] 火恐龍樹無 `火焰輪`、小火馬樹無 `火焰拳`（單元測試斷言）。
- [ ] 雙屬性寶可夢回傳 2 組軌道 + 1 組 Keystone。
- [ ] 全 18 屬性 × 全 tier 渲染無空節點（備援招兜底）。

## Phase 3：D4 配點與戰鬥結算 Hook（Battle Execution Engine）

**目標**：SP 配點的持久化、招式 Lv.1~5 強化、Lv.5 質變分支的結算。

### 3.1 SP 配點事件（沿用事件溯源）

```js
// 既有事件流（kpi_events）沿用，新增事件型別
const EVT_TYPES = {
  SKILL_ALLOCATE: 'SKILL_ALLOCATE',   // { node: 'fire:ATK:T2:火焰拳', sp: 2 }
  SKILL_UPGRADE:  'SKILL_UPGRADE',    // { node: '...', toLevel: 3, sp: 1 }
  SKILL_MODIFIER: 'SKILL_MODIFIER',   // { node: '...', branch: '熔核之拳', sp: 3 }
  SKILL_RESET:    'SKILL_RESET',      // 既有，全數返還
  MOVE_LEARN:     'MOVE_LEARN',       // 既有，實際 cost
};
```

### 3.2 配點驗證（Server-Side）

```js
function allocateSP(studentId, payload) {
  const state = replayEvents(studentId);          // 重播現有事件得目前 SP 餘額
  const cost = tierCost[payload.tier] ?? 0;
  if (state.spRemaining < cost) throw new Error('INSUFFICIENT_SP');
  if (!isNodeVisible(state.speciesId, payload.node)) throw new Error('MORPHOLOGY_MISMATCH');
  emitEvent({ type: 'SKILL_ALLOCATE', payload, sp: cost }); // 記實際 cost（與既有 MOVE_LEARN 修復一致）
  return newState(state, payload);
}
```

### 3.3 戰鬥結算 Hook

```js
// 戰鬥時依配點狀態計算「實際招式」
function getEffectiveMove(state, moveName) {
  const spec = TYPE_SPEC_V3[state.speciesType].MOVE_SPEC[moveName];
  const lv = state.moveLevels[moveName] ?? 1;
  const i = lv - 1;

  let eff = {
    name: moveName,
    type: spec.type,
    power: spec.growth.power[i],
    acc: spec.base.acc,
    burn: spec.growth.burn[i],
    critBonus: lv >= 4 ? 1 : 0,
  };

  // Lv.5 質變分支
  const branch = state.modifiers[moveName];
  if (lv >= 5 && branch) eff = applyModifier(eff, spec.lv5_modifiers[branch]);

  // Signature Affinity（個體差異層，最高優先）
  const aff = SPECIES_LEARNSET[state.speciesId].affinity?.[moveName];
  if (aff) eff = applyAffinity(eff, aff);

  return eff;
}

function applyModifier(eff, mod) {
  if (mod.damage_mult) eff.power = Math.round(eff.power * mod.damage_mult);
  if (mod.multi_hit)   eff.hits = { count: mod.multi_hit, perPower: mod.per_hit_power };
  if (mod.burn_layers) eff.burnLayers = mod.burn_layers;
  return eff;
}

// 承載既有 ULT 重命名
function resolveMoveName(state, moveName) {
  const ult = TYPE_SPEC_V3[state.speciesType].ULT_MAP[moveName];
  return ult && state.unlockedUlts.includes(moveName) ? ult : moveName;
}
```

### 3.4 Python 對照

```python
def get_effective_move(state, move_name):
    spec = TYPE_SPEC_V3[state["species_type"]]["MOVE_SPEC"][move_name]
    lv = state.get("move_levels", {}).get(move_name, 1)
    eff = {"power": spec["growth"]["power"][lv-1], "burn": spec["growth"]["burn"][lv-1]}
    branch = state.get("modifiers", {}).get(move_name)
    if lv >= 5 and branch:
        eff.update(spec["lv5_modifiers"][branch])
    return eff
```

### 3.5 驗收

- [ ] 招式升級成本、質變成本與帳目一致（沿用 08-17 的 SP 帳目稽核流程）。
- [ ] 事件重播（400 筆樣本）與即時狀態完全吻合。
- [ ] `SKILL_RESET` 100% 返還 SP（既有機制原樣）。
- [ ] 被遮蔽節點無法被配點（Server 端 `MORPHOLOGY_MISMATCH` 防護）。

## Phase 4：舊資料相容與安全回滾機制

**目標**：舊帳號、舊進度無痛轉移；任何改動可一鍵回滾。

### 4.1 功能旗標（Feature Flag）

```js
// config.remote（Firebase Remote Config）
const flags = { skillTreeV3: true, morphologyFilter: true, spEconomyV3: true };
// 前端/後端一律先檢查旗標再走新邏輯
if (flags.skillTreeV3) return resolveSkillTree(id);
return legacyResolveVariant(id);   // 舊演算法路徑保留
```

### 4.2 舊資料轉移（Variant Weight → 技能樹節點）

```js
// 將舊帳號既有的 variant weight 平滑轉為對應節點等級
function migrateLegacyAccount(state) {
  const migrated = [];
  for (const [move, weight] of Object.entries(state.legacyVariantWeights || {})) {
    const lv = clamp(weightToLevel(weight), 1, 5);          // weight 0.0~1.0 → Lv.1~5
    migrated.push(emit({ type: 'SKILL_UPGRADE', node: move, toLevel: lv, sp: 0, migrated: true }));
  }
  emit({ type: 'SKILL_MIGRATION_DONE', sourceVersion: 'v2', ... });
  return replayEvents(state.studentId);
}
```

### 4.3 安全回滾

- **資料層**：v3 僅新增欄位、不改寫舊欄位 → 直接拔旗標即可回到 v2 讀取路徑。
- **事件層**：所有 v3 事件都有 `sourceVersion` 標記；回滾時可選擇「保留事件（僅停用結算）」或「重播至最後 v2 快照（完全還原）」。
- **機制**：回滾不刪任何資料；`migrateLegacyAccount` 反向不可逆，但因為 v2 路徑仍能讀舊欄位，帳號永遠可用。

### 4.4 發布順序（Rollout Plan）

| 階段 | 內容 | 驗收 |
|------|------|------|
| R1 | Phase 1 資料遷移上線（僅加欄位） | 線上無報錯、舊功能全數通過 |
| R2 | Phase 2 Resolver 於 10% 流量開啟 | 火恐龍/小火馬樹差異測試通過 |
| R3 | Phase 3 配點與結算全量開啟（旗標 on） | SP 帳目稽核 100% 吻合 |
| R4 | Phase 4 舊帳號轉移 + 監控 1 週 | 回滾按鈕就緒、異常率 0 |

---

## 附錄

### A. 術語對照（V2 → V3）

| V2 | V3 | 變更 |
|----|----|------|
| `TIER_VARIANTS` | `TIER_MATRIX` | 由「演算法隨機選」改為「5 軌固定池 + 形態過濾」 |
| `TYPE_MOVE_POOL` / `generateLearnset()` | `FALLBACK_BY_TRACK` | 保留為備援兜底 |
| `ULT_VARIANTS` | `MOVE_SPEC.lv5_modifiers` | 質變分支取代隨機 ULT 變體 |
| 演算法判定變體權重 | `SKILL_ALLOCATE/UPGRADE/MODIFIER` 事件 | 權重轉為玩家可見節點與等級 |

### B. 與既有事件溯源模式之對接

既有系統已採用 `kpi_events` 事件溯源（MOVE_LEARN、SKILL_RESET 等）。v3 沿用同一架構，新增三類事件（ALLOCATE／UPGRADE／MODIFIER），並遵循 08-17 已確立的原則：**「樹與事件一律記實際 cost」**、**「重置測試一律寫 SKILL_RESET 事件而非直接改 roster」**。

### C. 已知風險與緩解

| 風險 | 緩解 |
|------|------|
| 800+ 隻 tags 標註工作量大 | 以「形態模板」批次標註（同物種族一次套用），人工抽查傳說級 |
| 玩家質疑「我的寶可夢為什麼不能學火焰拳」 | 遮蔽節點以 🔒 標示 + 顯示「生理不符」說明 |
| 60 SP 配點張力不足（全點完） | 提供 Reset 免費機制 + 多流派成就誘因 |
| 雙屬性跨系帳目複雜 | 沿用單一事件流重播，跨系分配不另立帳目 |
