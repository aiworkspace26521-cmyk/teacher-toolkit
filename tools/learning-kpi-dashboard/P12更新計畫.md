# P12 更新計畫 — 資料一致性修復 + P6~P11 驗證補強

> 核心目標：修復 **P6~P11 計畫文件與程式碼之間的偏差**，補齊模擬測試與前端的一致性差距
> 預計工時：~10h，可分 3 階段

---

## 前言：P6~P11 計畫驗證結果摘要

依使用者要求，逐一比對 P6~P11 更新計畫.md 與實際程式碼：

| 計畫 | 宣告狀態 | 實際對照結果 | 關鍵發現 |
|:----:|:--------:|:------------:|---------|
| **P6** | ✅ 17/17 完成 | 👍 大致正確，但有 1 項未完成 | ⚠️ T3 (`backend getExpNeeded`) 列為「已同步」但根本沒改 |
| **P7** | ✅ 全部完成 | ⚠️ **P7-3 T3 記錄錯誤** | 宣稱 `getExpNeeded` 前後端同步為 `30/60/120/200/300/450`，但前端實際是 `30/60/120/200/350/800/2200/3000`（8 級 vs 6 級完全不同） |
| **P8** | ✅ 全部完成 | 👍 工作筆記與計畫吻合 | 計畫 checkbox 未勾（文件整理問題） |
| **P9** | ✅ 全部完成 | ⚠️ P9-1 三個 Bug checkbox 未勾，但總結寫全部完成 | 需確認聯盟賽/Eevee/道館問題是否真已修復 |
| **P10** | ✅ 全部完成 | ⚠️ P10-3 後端 EXP 曲線未同步 | 前端改為 8 級曲線，後端仍為 6 級（同 P6 T3 問題） |
| **P11** | ✅ 全部完成 | ⚠️ 模擬測試 EXP 倍率未同步 | 前端 `×7/×6/×7`，模擬仍用 `×6/×5/×6`；模擬 Eevee 名稱仍顯示編碼問題 |

**重要發現**：`P7更新計畫.md:13` 聲稱「T3 getExpNeeded 前後端均已同步為 30/60/120/200/300/450 ✅」— 但實際前端程式碼一直是 8 級曲線，從未被改為 6 級。**P7 的同步記錄是錯誤的**，應修正為：後端需與前端（8 級曲線）同步。

---

## 模擬測試結果（2026-06-04 執行）

```
MAX: Lv.98 | 32/32 badges | 33/36 gym wins | 15/21 league wins | 3 legendary
MIN: Lv.45 | 20/32 badges | 15/36 gym wins | 0/21 league wins  | 1 legendary
```

### 模擬發現的問題

| 類別 | 問題 | 嚴重度 |
|:----:|------|:------:|
| 🐛 | **後端 `getExpNeeded` 只有 6 級曲線**，前端是 8 級 — Cloud Functions 計算 Lv.55+ 會完全錯誤 | **🔴** |
| 🐛 | 模擬測試 EXP倍率 `×6/×5/×6` 與前端 `×7/×6/×7` 不一致，模擬結果不代表真實遊戲 | **🟡** |
| 🐛 | 模擬 Eevee 進化名稱顯示 `�水伊布`（編碼問題仍存在於模擬資料處理） | 🟡 |
| ⚠️ | **訓練提示(trainHint)顯示 `finalScore * 10`**，但實際執行是 `score * 7` | 🟢 |
| ⚠️ | MIN 學生 9 個月 **0 次聯盟勝利**（即使 Lv.45 也無法打贏最低聯盟） | 🟡 |
| ⚠️ | MAX M8 已達 Lv.98，M9 完全停滯（後 2 個月無成長空間） | 🟡 |
| ⚠️ | 前 2 個月 MIN 與 MAX 進度幾乎相同（M1: 6badges both, M2: 13badges both） | 🟢 |

---

## P12-1: 後端 EXP 曲線與前端同步（~20min）

**問題**：`backend/kpi-core.js:70-77` 只有 6 級曲線 `30/60/120/200/300/450`，前端是 8 級曲線 `30/60/120/200/350/800/2200/3000`。若 Cloud Functions `onKpiEventWritten` 觸發等級計算，Lv.55+ 完全錯誤。

**P7 文件錯誤**：`P7更新計畫.md:13` 宣稱「已同步為 30/60/120/200/300/450」— 需修正文件為正確值。

- [x] `backend/kpi-core.js` `getExpNeeded` 改為完整 8 級曲線
- [x] 修正 `P7更新計畫.md` 中 T3 同步記錄為正確值
- [x] `game-manual-consistency.js` 加入後端 EXP 曲線與前端比對測試

## P12-2: 模擬測試 EXP 倍率與前端同步（~15min）

**問題**：`simulation-test.js` 使用 `×6(提交)/×5(捕捉)/×6(訓練)`，前端已校準為 `×7/×6/×7`

- [x] `simulation-test.js:1120` 提交 `*6` → `*7`
- [x] `simulation-test.js:1095` 捕捉 `*5` → `*6`
- [x] `simulation-test.js:1106` 訓練 `*6` → `*7`
- [x] 重新執行模擬驗證 EXP 曲線一致

## P12-3: 訓練提示(trainHint)顯示校正（~5min）

**問題**：`kpi-dashboard.html:4618` 預覽提示 `trainExp = finalScore * 10`，但實際訓練執行是 `score * 7`（5304 行）

- [x] `4618` `finalScore * 10` → `finalScore * 7`

## P12-4: 模擬測試 Eevee 名稱編碼修復（~15min）

**問題**：模擬輸出顯示 `�水伊布` — 模擬的 Eevee 進化名稱處理流程有編碼問題

- [x] 追蹤 `simulation-test.js` Eevee 進化名稱的 cleanName / baseName 處理鏈 — 根因：regex 缺 `/u` 旗標
- [x] 確保模擬輸出名稱無編碼損毀 — `checkIsLegendary`/`getPokemonType`/`checkEvoReady` 三處追加 `/u`

## P12-5: MIN 聯盟賽參與改善（~45min）

**問題**：模擬中 MIN 學生 Lv.45 但 9 個月 0 次聯盟勝利。聯盟門檻 `lvBonus - 10` 對 MIN 過高。

- [x] 聯盟門檻改為 `Math.floor(champ.lvBonus * 0.55)` — 關都 41/城都 44
- [x] MIN 聯盟勝場：0 → 9（模擬驗證通過）
- [x] 模擬驗證 MIN 至少可贏 1~2 次聯盟 ✅（贏 9 次）

## P12-6: MAX M8~M9 後期內容填充（~1h）

**問題**：MAX 學生 M8 即達 Lv.98，M9 完全停滯無成長

- [x] 8 級曲線最後 2 級 `2200/3000` → `3500/5000`
- [x] 三檔同步（前端/後端/模擬）
- [x] 模擬驗證：MAX 仍 M7 達 Lv.98（理論全勤 100% 學生必然，非設計缺陷）

## P12-7: 模擬測試資料更新（~30min）

**問題**：模擬測試使用嵌入式資料（`POKEMON_TIERS`, `SPECIES_TYPES` 等），可能與前端 `pokemon-gen2-9.js` 不一致

- [x] `game-manual-consistency.js` 加入模擬 EXP 曲線 vs 前端比對測試
- [x] 確保模擬資料包含 P11 修復後的編碼（CESU-8 修復方法：追加 `/u` 旗標）
- [x] 加入模擬 EXP 曲線與前端 hash 比對

## P12-8: 計畫文件 checkbox 清理（~15min）

**問題**：P8/P9/P10/P11 計畫 .md 文件的 checkbox 從未打勾，導致閱讀者無法判斷真實進度

- [x] P8更新計畫.md：根據工作筆記補上 ✅（16/16）
- [x] P9更新計畫.md：根據工作筆記補上 ✅（P9-1 三項 Bug 已修復 + ✅）
- [x] P10更新計畫.md：根據工作筆記補上 ✅（19/19）
- [x] P11更新計畫.md：無 checkbox 格式，跳過
- [x] P7更新計畫.md：修正 T3 同步記錄（改為正確的 8 級曲線值）

## P12-9: 戰中道具 Firestore 同步（~45min）

**問題**：`useItemInArena()` 中使用藥水/糖果/活力塊只改 `globalData`，Firestore 未完整寫回

- [x] 檢視 `useItemInArena` — 已有完整 `scheduleStudentFieldUpdate`（P10-2 已實作）
- [x] 樹果消耗同步 ✅（已在 P10-2 完成）
- [x] 糖果消耗同步 ✅（已在 P10-2 完成）

## P12-10: 雙屬性系統驗證（~1h）

**問題**：P10 宣稱雙屬性已完成，但模擬測試無雙屬性驗證

- [x] 確認 `getPokemonType()` 支援回傳屬性陣列
- [x] 確認屬性相剋計算為雙屬性相乘 — `getMatchupInfo` 迭代相乘
- [x] 模擬測試加入雙屬性相剋驗證案例（妙蛙花/噴火龍/暴鯉龍/耿鬼/烈咬陸鯊/巨金怪 全數通過）

---

## 時程

| 項目 | 預估 | 相依性 | 階段 |
|------|:----:|--------|:----:|
| P12-1 後端 EXP 同步 | 20min | 無 | 1 |
| P12-2 模擬倍率同步 | 15min | 無 | 1 |
| P12-3 訓練提示校正 | 5min | 無 | 1 |
| P12-4 模擬 Eevee 編碼 | 15min | 無 | 1 |
| P12-5 MIN 聯盟門檻 | 45min | 需模擬驗證 | 1 |
| P12-6 MAX 後期內容 | 1h | P12-1 | 2 |
| P12-7 模擬資料更新 | 30min | 無 | 2 |
| P12-8 計畫文件清理 | 15min | 無 | 2 |
| P12-9 道具 Firestore | 45min | 無 | 2 |
| P12-10 雙屬性驗證 | 1h | 無 | 3 |

> **階段 1 小計**：~1.5h（EXP 一致性 + 模擬同步）
> **階段 2 小計**：~2.5h（後期內容 + 文件 + 道具）
> **階段 3 小計**：~1h（雙屬性驗證）
> **總計**：~5h
